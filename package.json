{
  "name": "@giancosta86/typed-env",
  "version": "2.0.1",
  "description": "Functional library for type-safe environment variables",
  "keywords": [
    "typesafe",
    "environment",
    "variables",
    "functional",
    "NODE_ENV",
    "production",
    "Jest",
    "test",
    "flag"
  ],
  "homepage": "https://github.com/giancosta86/typed-env#readme",
  "bugs": "https://github.com/giancosta86/typed-env/issues",
  "license": "MIT",
  "author": {
    "name": "Gianluca Costa",
    "email": "gianluca@gianlucacosta.info",
    "url": "https://gianlucacosta.info/"
  },
  "files": [],
  "main": "dist",
  "types": "dist",
  "repository": "giancosta86/typed-env",
  "scripts": {
    "clean": "rimraf dist && rimraf coverage && rimraf giancosta86-*.tgz",
    "clean:all": "yarn clean && rimraf node_modules",
    "format": "prettier --write .",
    "prepack": "yarn check-all && yarn build && yarn test:prod",
    "check-all": "yarn check-format && yarn lint && yarn test",
    "check-format": "prettier --check .",
    "lint": "eslint . --ext .js,.ts",
    "test": "jest",
    "test:watch": "yarn test --watch",
    "test:coverage": "yarn test --coverage",
    "test:prod": "jest dist --testPathIgnorePatterns=[]",
    "build": "yarn compile-ts",
    "compile-ts": "tsc",
    "watch": "yarn compile-ts --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@swc/core": "^1.2.218",
    "@swc/jest": "^0.2.22",
    "@types/jest": "^28.1.4",
    "@types/node": "^16",
    "@typescript-eslint/eslint-plugin": "^5.30.3",
    "@typescript-eslint/parser": "^5.30.3",
    "eslint": "^8.18.0",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": ">=16.16.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
